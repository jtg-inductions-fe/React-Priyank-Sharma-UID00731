import { LoginForm } from '@features';

import { StyledPageContainer } from './Login.styles';

export const Login = () => (
    <StyledPageContainer>
        <LoginForm />
    </StyledPageContainer>
);
