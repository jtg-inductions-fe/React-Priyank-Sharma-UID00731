import { LoginForm } from '@containers';

import { StyledPageContainer } from './Login.styles';

export const Login = () => (
    <StyledPageContainer>
        <LoginForm />
    </StyledPageContainer>
);
