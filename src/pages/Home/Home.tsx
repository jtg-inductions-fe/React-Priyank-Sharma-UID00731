import { Footer, Header, Hero, RestaurantSection } from '@components';

const Home = () => (
    <>
        <Header />
        <Hero />
        <RestaurantSection />
        <Footer />
    </>
);

export default Home;
