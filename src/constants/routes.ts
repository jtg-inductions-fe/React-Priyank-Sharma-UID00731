/**
 * Application route paths.
 */
export const APP_ROUTES = {
    HOME: '/',
    RESTAURANTS: '/restaurants',
    MENU: '/menu',
    LOGIN: '/login',
    REGISTER: '/register',
    DASHBOARD: '/dashboard',
    PROFILE: '/profile',
    NOT_FOUND: '*',
} as const;
