import type { BreakpointsOptions } from '@mui/material/styles';

/**
 * Application Breakpoints
 * Defines responsive boundaries in pixels (px) for a mobile-first approach.
 */
export const breakpoints: BreakpointsOptions = {
    values: {
        xs: 0, // Mobile
        sm: 600, // Tablet
        md: 900, // Laptop
        lg: 1200, // Desktop
        xl: 1536, // Large Display
    },
};
